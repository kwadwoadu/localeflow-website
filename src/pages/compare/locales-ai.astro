---
import BaseLayout from '../../layouts/BaseLayout.astro';
import Header from '../../components/Header.astro';
import Footer from '../../components/Footer.astro';
import { VsComparison } from '../../components/compare/VsComparison';
import { FAQ } from '../../components/FAQ';
import { localflow, extendedCompetitors } from '../../data/competitors';
import { ArrowLeft, ArrowRight } from 'lucide-react';

const competitor = extendedCompetitors.find(c => c.slug === 'locales-ai')!;
const pageTitle = `LocaleFlow vs ${competitor.name} | Shopify Translation App Comparison`;
const pageDescription = `Compare LocaleFlow and ${competitor.name} for Shopify translation. See features, pricing, and which app is right for your store.`;

const faqItems = [
  {
    question: `What's the main difference between LocaleFlow and ${competitor.name}?`,
    answer: `${competitor.name} is an AI-native app with aggressive pricing (free tier, $9.99 starter) and GPT-4 translations. LocaleFlow uses Claude AI with full metaobject support and automatic sync. If you need the cheapest option, ${competitor.name} wins on price. For comprehensive translation with premium quality, LocaleFlow delivers more value.`
  },
  {
    question: `Does ${competitor.name} support all content types?`,
    answer: `${competitor.name} covers basic content but lacks metaobject support. If you have size guides, store locators, or custom product specs in metaobjects, they won't be translated. LocaleFlow fully supports metaobject translation.`
  },
  {
    question: `Which app is easier to set up?`,
    answer: `Both apps are easy to set up - they're AI-native and designed for simplicity. ${competitor.name} has a quick 1-click translate, while LocaleFlow offers a guided setup with market selection and preview before publishing.`
  },
  {
    question: `Is ${competitor.name}'s free tier really free?`,
    answer: `Yes, ${competitor.name} offers genuinely free AI translation for small stores. The paid tiers unlock more features. LocaleFlow doesn't have a free tier but offers a 7-day trial with full features.`
  }
];
---

<BaseLayout title={pageTitle} description={pageDescription}>
  <Header />

  <main class="pt-20">
    <section class="section bg-gradient-to-b from-primary-50 to-white">
      <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div class="mb-8 reveal">
          <a href="/compare" class="inline-flex items-center gap-2 text-gray-600 hover:text-primary-600 transition-colors">
            <ArrowLeft className="w-4 h-4" />
            Back to all comparisons
          </a>
        </div>

        <div class="text-center mb-12 reveal">
          <span class="inline-block px-4 py-1.5 bg-primary-100 text-primary-700 rounded-full text-sm font-medium mb-4">
            App Comparison
          </span>
          <h1 class="font-display text-4xl md:text-5xl font-bold text-gray-900 mb-6">
            LocaleFlow vs {competitor.name}
          </h1>
          <p class="text-xl text-gray-600 max-w-3xl mx-auto">
            {competitor.name} offers aggressive pricing with GPT-4 AI and {competitor.reviews.toLocaleString()}+ reviews.
            See how it compares to LocaleFlow's comprehensive approach.
          </p>
        </div>

        <div class="grid md:grid-cols-2 gap-6 max-w-2xl mx-auto reveal stagger-1">
          <div class="bg-white rounded-xl p-6 shadow-sm border border-primary-200 text-center">
            <div class="text-sm text-gray-500 mb-1">LocaleFlow</div>
            <div class="text-2xl font-bold text-primary-600">{localflow.rating} stars</div>
            <div class="text-sm text-gray-400">{localflow.reviews}+ reviews</div>
          </div>
          <div class="bg-white rounded-xl p-6 shadow-sm border border-gray-200 text-center">
            <div class="text-sm text-gray-500 mb-1">{competitor.name}</div>
            <div class="text-2xl font-bold text-gray-700">{competitor.rating} stars</div>
            <div class="text-sm text-gray-400">{competitor.reviews.toLocaleString()}+ reviews</div>
          </div>
        </div>
      </div>
    </section>

    <section class="section bg-white">
      <div class="max-w-4xl mx-auto px-4 sm:px-6 lg:px-8">
        <div class="reveal">
          <VsComparison client:visible localeflow={localflow} competitor={competitor} />
        </div>
      </div>
    </section>

    <section class="section bg-gray-50">
      <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div class="text-center mb-12 reveal">
          <h2 class="font-display text-3xl md:text-4xl font-bold text-gray-900 mb-4">Key Differences</h2>
        </div>

        <div class="grid md:grid-cols-2 gap-8 reveal stagger-1">
          <div class="bg-white rounded-xl p-8 shadow-sm border border-gray-100">
            <h3 class="font-display text-xl font-semibold text-gray-900 mb-4">{competitor.name}'s Approach</h3>
            <ul class="space-y-3">
              {competitor.strengths.map((strength) => (
                <li class="flex items-start gap-2 text-gray-600">
                  <span class="w-5 h-5 rounded-full bg-gray-200 flex items-center justify-center shrink-0 mt-0.5 text-xs">+</span>
                  <span>{strength}</span>
                </li>
              ))}
              {competitor.weaknesses.map((weakness) => (
                <li class="flex items-start gap-2 text-gray-600">
                  <span class="w-5 h-5 rounded-full bg-red-100 text-red-500 flex items-center justify-center shrink-0 mt-0.5 text-xs">-</span>
                  <span>{weakness}</span>
                </li>
              ))}
            </ul>
          </div>

          <div class="bg-primary-50 rounded-xl p-8 shadow-sm border border-primary-100">
            <h3 class="font-display text-xl font-semibold text-gray-900 mb-4">LocaleFlow's Approach</h3>
            <ul class="space-y-3">
              {localflow.strengths.map((strength) => (
                <li class="flex items-start gap-2 text-gray-700">
                  <span class="w-5 h-5 rounded-full bg-primary-200 text-primary-700 flex items-center justify-center shrink-0 mt-0.5 text-xs">✓</span>
                  <span>{strength}</span>
                </li>
              ))}
            </ul>
          </div>
        </div>
      </div>
    </section>

    <section class="section bg-white">
      <div class="max-w-3xl mx-auto px-4 sm:px-6 lg:px-8">
        <div class="text-center mb-12 reveal">
          <h2 class="font-display text-3xl md:text-4xl font-bold text-gray-900 mb-4">Frequently Asked Questions</h2>
        </div>
        <div class="reveal stagger-1">
          <FAQ client:visible items={faqItems} />
        </div>
      </div>
    </section>

    <section class="section bg-gradient-to-r from-primary-500 to-orange-500">
      <div class="max-w-4xl mx-auto px-4 sm:px-6 lg:px-8 text-center reveal">
        <h2 class="font-display text-3xl md:text-4xl font-bold text-white mb-6">Ready to Try LocaleFlow?</h2>
        <p class="text-xl text-white/90 mb-8">Start your 7-day free trial. No credit card required.</p>
        <div class="flex flex-col sm:flex-row gap-4 justify-center">
          <a href="https://apps.shopify.com/localeflow" target="_blank" rel="noopener noreferrer" class="btn-primary bg-white text-primary-600 hover:bg-gray-100 inline-flex items-center justify-center gap-2">
            Start Free Trial
            <ArrowRight className="w-5 h-5" />
          </a>
          <a href="/compare" class="btn-secondary border-white/30 text-white hover:bg-white/10 inline-flex items-center justify-center gap-2">
            Compare More Apps
          </a>
        </div>
      </div>
    </section>
  </main>

  <Footer />
</BaseLayout>
